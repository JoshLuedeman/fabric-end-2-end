# IoT sensor telemetry — Event Hub connection
EVENT_HUB_CONNECTION_STRING=
EVENT_HUB_NAME=tales-and-timber-events
DRY_RUN=true
IOT_EVENTS_PER_SECOND=10
#
# NOTE: POS and inventory event generation has moved to the OLTP simulator
# (simulator/).  Those events reach Eventhouse via Fabric SQL Database
# Change Event Streaming → Eventstream — no separate generators needed.
